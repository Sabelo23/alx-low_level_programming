#include <stdio.h>
/**
 * main - program that prints the alphabets in lowercase, follwed by a new line
 *
 * Return: always 0
 */
int main(void)
{
	for (i = 'a' ; i <= 'z' ; i++)
		putchar(i);
	putchar('\n');
	return (0);
}
